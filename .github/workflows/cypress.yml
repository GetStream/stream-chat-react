name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          browser: chrome
          headless: true
          build: npm run build
          start: npm start
          run:
            cd ../stream-chat-react/examples/typescript-app/ && yarn;yarn start
            pwd
            ls
          wait-on: "http://localhost:3000"
          
          
