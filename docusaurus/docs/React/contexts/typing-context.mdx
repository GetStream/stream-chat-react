---
id: typing_context
sidebar_position: 8
title: TypingContext
---

## Overview

The `TypingContext` wraps the entire application via the `Channel` component and exposes the hook `useTypingContext`.

## Basic Usage

```jsx
const { typing } = useTypingContext();
```

## Value

### typing

An array of users who are typing at any given time.

| Type                         |
| ---------------------------- |
| StreamChannelState['typing'] |